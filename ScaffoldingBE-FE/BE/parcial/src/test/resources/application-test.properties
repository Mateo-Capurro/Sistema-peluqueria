# Perfil de tests: DB en memoria, aislada y efímera.
# Sobrescribe la datasource de archivo del main para no tocar ~/peluqueria-db
# ni romper el aislamiento entre tests (cobertura 95%).
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=create-drop
